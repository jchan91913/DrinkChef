<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comp3717.comp3717">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LiquorSelectActivity" />
        <activity android:name=".MixerSelectActivity" />
        <activity android:name=".GarnishSelectActivity" />
        <activity android:name=".RecipeSelectAcitvity" />
        <activity android:name=".DescriptionActivity"></activity>
    </application>

</manifest>